public class HashTable
{
    //hashtables work using hashfunctions
    //if the key is a string, the hashfunction does this...
    //string -> hashcode -> index
    //the string goes through some function which creates a hashcode(some integer), which then is mapped to an index in an array

}
